import React from 'react';

import {Container} from 'react-bootstrap';

import Balance from '../app/http/partials/balance';

export default () => {
  return (
    <div className="container-fluid p-0">
        <Balance/>
    </div>
  );
};